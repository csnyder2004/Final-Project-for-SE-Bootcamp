# ==============================================
# 🗄️  MongoDB Atlas Connection
# ==============================================
# Replace <username> and <password> with your MongoDB Atlas credentials.
# Replace <cluster-url> with your cluster hostname (e.g. cluster0.x1iglpz)
# Replace <dbname> with the database name you want to use.

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>.mongodb.net/<dbname>?retryWrites=true&w=majority


# ==============================================
# 🔐  JWT Secret
# ==============================================
# Used to sign and verify authentication tokens (JWT).
# Use a long, random string here — never commit the real one!

JWT_SECRET=your_jwt_secret_here


# ==============================================
# ⚙️  Server Configuration
# ==============================================
# Local development port (Render or hosting will override automatically)

PORT=4000


# ==============================================
# 🌍  Environment
# ==============================================
# Optional: "development" for local, "production" for deployed environments.

NODE_ENV=development
